fun main () {

    //println(getType(User()))
    //println(getType(AdminUser()))

    }
    //getType({5})
    //getType(null)
    //getType(arrayOf{4})
    //getType("Hello")
    //getType(false)
    //getType(5)
    //getType('a')
    //getType(listOf(5))
    //getType(mutableSetOf<Int>())
    //getType(mutableSetOf(4))


//Note 14  create 2 custom types one of them extends the other ( parent vs child ) check if values printed are the same
// or not ( solve the problem in which the two shouldn't be the same )